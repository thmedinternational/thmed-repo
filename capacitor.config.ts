import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thmedinternational.app',
  appName: 'TH-MED International',
  webDir: 'dist'
};

export default config;